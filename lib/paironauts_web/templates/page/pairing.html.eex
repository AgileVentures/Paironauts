<%# We can pop this into a common view, common controller action, and pop in 
    something in the assigns that gets populated from a UI element interaction 
    
    Duplicating for now -- safer and lets us use the right abstraction when it emerges %>
<div id="wait">Waiting for pair partner...</div>
<script src="<%= static_path(@conn, "/js/socket.js") %>"></script>
